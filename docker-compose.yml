version: "3.7"
services:
  spark-master:
    image: spydernaz/spark-master:latest
    ports:
      - "8080:8080"
      - "7077:7077"
    volumes:
       - ./apps:/opt/spark-apps
       - ./data:/opt/spark-data
    environment:
      - "SPARK_LOCAL_IP=spark-master"
  spark-worker:
    image: spydernaz/spark-worker:latest
    depends_on:
      - spark-master
    environment:
      - SPARK_MASTER=spark://spark-master:7077
      - SPARK_WORKER_CORES=1
      - SPARK_WORKER_MEMORY=2G
      - SPARK_DRIVER_MEMORY=1G
      - SPARK_EXECUTOR_MEMORY=1G
    volumes:
       - ./apps:/opt/spark-apps
       - ./data:/opt/spark-data
  
  pyspark-notebook:
    image: jupyter/pyspark-notebook-updated     
    container_name: pyspark_notebook-updated
    ports:
      - "8888:8888"
    volumes:
      - ./work:/home/jovyan/work
    environment:
      - PYSPARK_PYTHON=/usr/bin/python
